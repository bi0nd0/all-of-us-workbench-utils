version: alternate-publication-1
reports:
  main:
    study_id: alternate_demo
    protocol_version: '1'
    group_sizes: true
    characteristics:
      age: [mean_sd, missing]
      exclusion_condition: [n_percent, missing]
      ehr_days: [mean_sd, missing]
    associations:
      primary: [example_condition]
    tables:
      flow: [Stage, Participants]
      balance: [Variable, Absolute SMD before, Absolute SMD after]
